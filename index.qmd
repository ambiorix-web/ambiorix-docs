---
pagetitle: "Ambiorix — a minimalist web framework for R"
description: "Unopinionated, minimalist web framework for R, inspired by express.js."
page-layout: full
toc: false
---

::: {.hero}

::: {.hero-copy}

[a web framework for R]{.eyebrow}

# Build for the web, with R. {.hero-title}

::: {.hero-lead}
Ambiorix is an unopinionated, minimalist web framework inspired by
express.js. Routing, middleware, templating, websockets and async —
one syntax, all in R.
:::

:::

::: {.hero-code}

```r
library(ambiorix)

app <- Ambiorix$new(port = 3000L)

app$get("/", function(req, res) {
  res$send("Hello, World!")
})

app$start()
```

:::

::: {.hero-actions}
[Get started](/docs/getting-started/){.btn .btn-primary}
[Read the book](https://book.ambiorix.dev/){.btn .btn-secondary}
[Watch the video](https://www.youtube.com/watch?v=owpbIQ-j6Kk){.btn .btn-secondary}
:::

:::

::: {.home-section .section-tight}

::: {.section-head}

::: {.section-head-main}
[Install]{.section-eyebrow}

## Up and running in two commands. {.section-title}
:::

::: {.section-head-aside}
The stable release is on CRAN. The development version lives on GitHub.
:::

:::

::: {.install-grid}

::: {.install-step}
[1 — install the package]{.step-label}

```r
install.packages("ambiorix")
```
:::

::: {.install-step}
[2 — run your app]{.step-label}

```bash
Rscript app.R
```
:::

:::

:::

::: {.home-section}

::: {.section-head}

::: {.section-head-main}
[Features]{.section-eyebrow}

## Everything you need to serve R over HTTP. {.section-title}
:::

::: {.section-head-aside}
Nothing here is mandatory. Use the pieces you need and ignore the rest.
:::

:::

::: {.feature-grid}

::: {.feature}
### Routing
Build multipage applications right out of the box.
:::

::: {.feature}
### Templating
For server-side rendering. HTML, markdown, pug, and more.
:::

::: {.feature}
### Middleware
Easily pre-process every request to the server.
:::

::: {.feature}
### Websockets
For when you need bi-directional communication between server and client.
:::

::: {.feature}
### Async
Use asynchronous programming by returning promises from request handlers.
:::

::: {.feature}
### Autonomy
You have absolute, full control over the request-response cycle.
:::

:::

:::

::: {.home-section .section-major}

::: {.section-head}

::: {.section-head-main}
[In practice]{.section-eyebrow}

## A whole app, in one file. {.section-title}
:::

::: {.section-head-aside}
Middleware, static files, path parameters and a JSON endpoint — under thirty lines.
:::

:::

::: {.showpiece}

```r
library(ambiorix)

app <- Ambiorix$new(port = 3000L)

# middleware runs on every request
app$use(function(req, res) {
  req$received_at <- Sys.time()
})

# serves index.html from the same directory
app$get("/", function(req, res) {
  res$send_file("index.html")
})

# path parameters
app$get("/users/:id", function(req, res) {
  res$json(list(id = req$params$id))
})

# POST with a JSON body
app$post("/users", function(req, res) {
  body <- parse_json(req)
  res$json(list(created = body))
})

app$start()
```

:::

:::

::: {.home-section}

::: {.section-head}

::: {.section-head-main}
[Going further]{.section-eyebrow}

## Templating, websockets and async. {.section-title}
:::

::: {.section-head-aside}
Server-side rendering, bi-directional messaging, and handlers that never block.
:::

:::

::: {.panel-tabset}

## Templating

```r
library(ambiorix)

app <- Ambiorix$new(port = 3000L)

app$get("/:book", function(req, res) {
  res$render(
    "home.html",
    data = list(title = req$params$book)
  )
})

app$start()
```

```html
<!-- templates/home.html -->
<!DOCTYPE html>
<html lang="en">
<head>
  <title>[% title %]</title>
</head>
<body>
  <h1>[% title %]</h1>
  <p>Welcome to Ambiorix!</p>
</body>
</html>
```

## Websockets

```r
library(ambiorix)

app <- Ambiorix$new(port = 3000L)

app$get("/", function(req, res) {
  res$send_file("home.html")
})

# listen to incoming websocket messages
app$receive("hello", function(msg, ws) {
  print(msg)
  ws$send("hello", "Hello back! (sent from R)")
})

app$start()
```

## Async

```r
library(ambiorix)
library(future)

plan(multisession)

now <- function() {
  format(
    x = Sys.time(),
    format = "%F %T",
    usetz = TRUE
  )
}

app <- Ambiorix$new(port = 3000L)

app$get("/slow", function(req, res) {
  future({
    Sys.sleep(10)
    res$send(now())
  })
})

app$get("/fast", function(req, res) {
  res$send(now())
})

app$start()
```

:::

:::

::: {.closer}

## Ready when you are. {.closer-title}

::: {.closer-lead}
Install the package, write a handler, start the server.
:::

::: {.closer-actions}
[Get started](/docs/getting-started/){.btn .btn-primary}
[Browse the gallery](/showcase/){.btn .btn-secondary}
:::

:::
